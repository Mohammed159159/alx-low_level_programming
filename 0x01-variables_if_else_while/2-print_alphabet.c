#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Alphabet in lower casse and 0 (Success)
 */

int main(void)
{
	for (char i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}

	putchar('\n');

	return (0);
}
